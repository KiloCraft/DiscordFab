/*
package com.github.hansi132.discordfab.discordbot.listener;

import com.github.hansi132.discordfab.DiscordFab;
import org.jetbrains.annotations.NotNull;
import org.kilocraft.essentials.api.event.EventHandler;
import org.kilocraft.essentials.api.event.player.OnSocialSpyWarning;

public class SocialSpyWarningListener implements EventHandler<OnSocialSpyWarning> {

    @Override
    public void handle(@NotNull OnSocialSpyWarning event) {
        DiscordFab.getInstance().getChatSynchronizer().onSocialSpyWarning(event.getSource(), event.getReceiver(), event.getMessage(), event.getMarked());
    }

}
*/
